import 'package:flutter/material.dart';

class IndividualEntry extends StatelessWidget {
  const IndividualEntry({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child:Image(image: AssetImage('assets/images/journal_bg2 copy.jpg'),)
      
    );
  }
}